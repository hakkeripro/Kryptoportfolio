# Copy these to apps/api/.env (or repo root .env) for local dev.

PORT=8788
HOST=0.0.0.0
JWT_SECRET=dev-secret-change-me
DB_FILE=./data/kp.sqlite
CORS_ORIGIN=http://localhost:5173

# ALERT_RUNNER_INTERVAL_SEC=30

# Push (optional)
# VAPID_PUBLIC_KEY=
# VAPID_PRIVATE_KEY=
# VAPID_SUBJECT=mailto:you@example.com
