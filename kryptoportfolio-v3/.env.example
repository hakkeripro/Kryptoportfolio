# Local development defaults

# --- Local Fastify API (apps/api)
PORT=8788
HOST=0.0.0.0
JWT_SECRET=dev-secret-change-me
DB_FILE=./data/kp.sqlite
CORS_ORIGIN=http://localhost:5173

# Optional: CoinGecko (local API) / hosted functions
# COINGECKO_DEMO_API_KEY=
# COINGECKO_BASE_URL=https://api.coingecko.com/api/v3

# Optional: Push (server-side alerts)
# VAPID_PUBLIC_KEY=
# VAPID_PRIVATE_KEY=
# VAPID_SUBJECT=mailto:you@example.com

# --- Web (apps/web)
# Override Vite proxy target (defaults to http://127.0.0.1:8788)
# VITE_API_PROXY_TARGET=http://127.0.0.1:8788

# --- Hosted (Cloudflare Pages Functions)
# Used when running Pages Functions locally with `wrangler pages dev` or in hosted env.
# DATABASE_URL=postgresql://...
# JWT_SECRET=...
# TEST_MODE=0
